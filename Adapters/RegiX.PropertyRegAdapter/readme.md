# RegiX.PropertyRegAdapter

Added SiteID (Идентификатор на служба по вписванията) to MetaData of GetPropertyInfo operation.

Added new versions (V2) for GetEntityInfo/ GetPersonInfo / GetPropertyInfo containing two new fields - WorthCurrency and WorthValue
